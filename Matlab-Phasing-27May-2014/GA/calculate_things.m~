function [ output_args ] = calculate_things(pn)
%UNTITLED4 Summary of this function goes here
%   Detailed explanation goes here

L1_norm=sum(abs(pn(:)));

sharpness=sum(abs(pn(:)).^4);

summed_phase=sum_phase(pn);

SS=shrink_wrap(pn,.2,.5);
area=


end

